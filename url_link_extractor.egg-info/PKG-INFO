Metadata-Version: 2.4
Name: url-link-extractor
Version: 0.1.0
Summary: URL 链接标题批量提取组件：按 URL 前缀发现匹配网页链接并提取 <title>，导出 result.xlsx
Author: url-link-extractor
License: MIT
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: httpx<0.28.0,>=0.27.0
Requires-Dist: selectolax<0.4.0,>=0.3.0
Requires-Dist: yarl<2.0.0,>=1.9.0
Requires-Dist: openpyxl<4.0.0,>=3.1.0
Provides-Extra: dev
Requires-Dist: pytest<9.0.0,>=8.0.0; extra == "dev"
Requires-Dist: pytest-asyncio<0.25.0,>=0.23.0; extra == "dev"
Requires-Dist: respx<0.22.0,>=0.21.0; extra == "dev"
Requires-Dist: ruff<0.7.0,>=0.5.0; extra == "dev"

# url-link-extractor

URL 链接标题批量提取组件：按指定 URL 前缀批量发现所有匹配网页链接，提取每个网页的 `<title>` 标题，输出"链接-标题"配对结果集，并导出为 `result.xlsx` Excel 文件。

## 组件定位

- **核心职责**：根据 URL 前缀，批量发现并提取所有匹配该前缀的网页链接及其 `<title>` 文本
- **核心输出**：链接-标题结果集 + 提取统计 + 异常报告 + `result.xlsx` 导出文件
- **技术栈**：Python 3.10+ / asyncio + httpx + selectolax + yarl + openpyxl

## 安装

```bash
pip install -e .
```

生产依赖：
```bash
pip install -r requirements.txt
```

开发依赖（含测试）：
```bash
pip install -r requirements-dev.txt
```

## Python API 调用

```python
from url_link_extractor import extract, ExtractRequest

request = ExtractRequest(
    url_prefix="https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/",
    entry_urls=["https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/"],
    concurrency=5,
    sort_by=None,  # 默认按 URL 排序
)

result = extract(request)

print(f"任务 ID：{result.task_id}")
print(f"统计：total={result.statistics.total} success={result.statistics.success} "
      f"no_title={result.statistics.no_title} failed={result.statistics.failed}")
print(f"导出路径：{result.export_path}")

for record in result.result_set:
    print(f"  [{record.status.value}] {record.url}  |  {record.title or '(缺失)'}")
```

## CLI 用法

```bash
# 基本用法
python -m url_link_extractor --url-prefix https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/

# 指定发现入口 + 并发度 + 排序
python -m url_link_extractor \
    --url-prefix https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/ \
    --entry-url https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/ \
    --concurrency 10 \
    --sort-by url \
    --work-dir ./output

# 允许跨子域 + 不跟随重定向
python -m url_link_extractor \
    --url-prefix https://support.huaweicloud.com/intl/zh-cn/usermanual-cli/ \
    --allow-cross-subdomain \
    --no-follow-redirect
```

## 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `url_prefix` | str | 必填 | URL 前缀，必须为合法 HTTPS URL，长度 ≤ 2048 |
| `entry_urls` | list[str] | None | 发现入口 URL 列表，每项合法 URL，大小 ≤ 20 |
| `allow_cross_subdomain` | bool | False | 是否允许跨子域（仍禁止跨主域） |
| `follow_redirect` | bool | True | 是否跟随重定向，跳数上限 5 |
| `concurrency` | int | 5 | 并发度，范围 [1, 20] |
| `sort_by` | SortBy | URL | 排序方式：url / title / none |
| `work_dir` | str | None | result.xlsx 工作目录，None 表示当前目录 |

## result.xlsx 输出

| 列 | 说明 |
|----|------|
| URL 完整链接 | 绝对完整 URL（含协议、主机、完整路径），禁止省略号或相对路径 |
| 页面标题 | 规整后的 `<title>` 文本；标题缺失时为空 |

- 文件名固定为 `result.xlsx`，不可改名
- 同名文件覆盖写（不追加合并）
- 空结果集仅含表头两列
- 行顺序与结果集排序一致

## 错误码表

| 错误码 | 说明 |
|--------|------|
| `TIMEOUT` | 网页拉取超时 |
| `HTTP_ERROR` | 网页返回非 2xx 状态码或非 HTTPS |
| `PARSE_ERROR` | HTML 解析失败 |
| `NO_TITLE` | 标题缺失 |
| `SSRF_BLOCKED` | SSRF 拦截（链接指向内网/本地地址） |
| `ENTRY_UNREACHABLE` | 发现入口不可访问 |
| `INVALID_PREFIX` | URL 前缀格式非法 |
| `STATS_INCONSISTENT` | 统计守恒破坏 |
| `EXPORT_WRITE_ERROR` | result.xlsx 导出写入失败 |

## 架构

```
url_link_extractor/
├── core/           # 业务编排与规则层
│   ├── prefix_validator.py
│   ├── entry_resolver.py
│   ├── link_discoverer.py
│   ├── title_extractor.py
│   ├── result_aggregator.py
│   └── result_exporter.py
├── infra/          # 外部依赖封装层
│   ├── logger.py
│   ├── url_normalizer.py
│   ├── ssrf_guard.py
│   ├── http_client.py
│   ├── html_parser.py
│   └── excel_writer.py
├── entry/          # 流水线编排入口
│   └── extractor.py
├── models/         # 领域数据模型
└── cli.py          # CLI 入口
```

流水线顺序：`PrefixValidator → LinkDiscoverer → TitleExtractor → ResultAggregator → ResultExporter`

## 测试

```bash
pytest tests/ -v
```

## 安全约束

- 仅允许 HTTPS 协议拉取目标网页
- SSRF 防护：DNS 解析后 IP 锁定，拒绝内网/回环/链路本地/保留/组播地址
- 日志不输出 HTTP 响应体、HTML 正文、`<title>` 内容等敏感字段
- 所有输出 URL 均为绝对完整 URL（含协议、主机、完整路径）
